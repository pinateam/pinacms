{strip}
	{if $meta.meta_title}<title>{$meta.meta_title}</title>{/if}
	{if $meta.meta_keys}<meta name="keywords" content="{$meta.meta_keys}" />{/if}
	{if $meta.meta_description}<meta name="description" content="{$meta.meta_description}" />{/if}
{/strip}